# ${package_name}

WorkerTransfer library package.

## Usage

```bash
uv sync --all-packages
```

See `docs/architecture.md` for the sharing rule and `docs/ULTRAPLAN.md` for the
role of `worker-*` libraries in the kernel-vs-bausteine layering (ADR-0002).
