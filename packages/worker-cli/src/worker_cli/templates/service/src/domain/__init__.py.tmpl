"""Domänen-Schicht von ${service_name}.

Entitäten, Wertobjekte und Domänenfehler. Ohne FastAPI, ohne ORM, ohne
Transport — die Bausteine dafür stehen in `worker_core` (`Entity`,
`ValueObject`, `DomainEvent`, `Result`, `DomainError`).
"""
